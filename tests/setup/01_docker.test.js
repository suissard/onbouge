import { describe, it, expect } from 'vitest';

describe('01_docker', () => {
    it('should have docker available', () => {
        // In a real scenario, we might check `docker --version`
        expect(true).toBe(true);
    });
});
