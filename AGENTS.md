# Agent Instructions

Before committing any changes, please follow these steps:

1.  **Build the frontend:** Run `npm run build` within the `frontend` directory to ensure the project builds successfully.
